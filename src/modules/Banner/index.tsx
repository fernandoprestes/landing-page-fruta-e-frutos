import style from './banner.module.scss'

export const Banner = () => {
  return (
    <section className={style.banner}>
      <h2 className={style.banner__title}>Reaproveite melhor os alimentos</h2>
    </section>
  )
}
